name: sqd-orbital-ordering
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pyscf
  - openmpi
  - llvm-openmp
  - compilers
  - pip
  - pip:
      - ffsim
      - qiskit
      - qiskit-aer
      - qiskit-addon-sqd
      - matplotlib
      - pandas
      - scipy
      - pytest
