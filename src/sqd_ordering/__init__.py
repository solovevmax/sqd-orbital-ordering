"""sqd_ordering -- shared library for the SQD orbital-ordering project.

mask.py: the LUCJ heavy-hex locality mask (single source of truth).
scores.py: amplitude-derived, non-oracle scoring functions.
sampling.py: Aer sampling and top-k determinant selection.
sbd.py: thin wrapper around the external sbd binary.
"""
